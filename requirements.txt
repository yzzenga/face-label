# FaceLabel 依赖清单
# 安装方式:
#   CPU 环境:  pip install -r requirements.txt
#   GPU 环境:  pip install -r requirements.txt -r requirements-gpu.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
insightface>=0.7.3
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
aiofiles>=23.2.0
python-multipart>=0.0.6

# ── ONNX Runtime ──
# 默认安装 CPU 版本，兼容所有环境
# 如果有 NVIDIA GPU + CUDA，请额外安装 GPU 版:
#   pip install onnxruntime-gpu>=1.15.0
# 系统会自动检测并优先使用 GPU
onnxruntime>=1.15.0